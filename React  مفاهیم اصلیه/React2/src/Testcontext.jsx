import React, { createContext } from "react";

export const testContext = createContext({})

export const colorContext = createContext("")

export class Conrext extends React.Component{
    render(){
        return(
            <h4>sepas gozaram </h4>
        )
    }
}